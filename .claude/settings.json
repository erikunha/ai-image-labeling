{
  "skipPermissions": true,
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,sys; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path',''); sys.exit(0 if not f.endswith(('.ts','.tsx')) else 1)\" 2>/dev/null && exit 0 || pnpm run typecheck 2>&1 | tail -5"
          }
        ]
      }
    ]
  }
}
